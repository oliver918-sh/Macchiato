# Macchiato 发布说明

这个文件夹已经是可以发布到 GitHub Pages 的静态网页。

## 推荐发布方式

1. 打开你已经创建好的 GitHub 仓库 `Macchiato`。
2. 把 `macchiato-site` 文件夹里的内容放到仓库根目录。
3. 提交并推送到 `main` 分支。
4. 进入 GitHub 仓库的 `Settings` -> `Pages`。
5. Source 选择 `Deploy from a branch`。
6. Branch 选择 `main`，文件夹选择 `/root`。
7. 保存后等待 1-3 分钟，GitHub 会生成一个朋友可以访问的网址。

## 发布前检查

- `index.html` 必须在仓库根目录。
- `assets` 文件夹必须一起上传。
- 不要只上传照片或只上传 HTML。
- 如果朋友打不开视频，优先检查视频文件是否完整上传。

## 当前素材筛选

- 照片：19 张精选图。
- 视频：6 段短视频。
- 首屏：三人行合照。
